# NixOS flake configuration .gitignore

# Ignore machine-specific hardware config (generated locally)
# Nix build artifacts
result
result-*

# Flake lock file (optional - some people commit it, some don't)
# flake.lock

# Oter API tokens (copy secrets/oter.env.example → secrets/oter.env; sourced by Waybar — see modules/sway.nix)
secrets/oter.env
