# Hardware configuration - GENERATED FILE
# 
# This file was generated by 'nixos-generate-config' and contains
# machine-specific hardware settings (filesystems, boot, CPU, etc.)
#
# To regenerate this file:
#   sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix
#
# NOTE: In flake-based configs, it's common practice to include this file
# in the flake directory for portability, even though it's generated.
# This makes the flake self-contained and version-controllable.
#
# If you prefer to keep it separate, you can reference it from /etc/nixos:
#   imports = [ /etc/nixos/hardware-configuration.nix ];
{ config, lib, pkgs, modulesPath, ... }:

{
  imports =
    [ (modulesPath + "/installer/scan/not-detected.nix")
    ];

  boot.initrd.availableKernelModules = [ "nvme" "xhci_pci" "usb_storage" "sd_mod" ];
  boot.initrd.kernelModules = [ ];
  boot.kernelModules = [ "kvm-amd" ];
  boot.extraModulePackages = [ ];

  fileSystems."/" =
    { device = "/dev/disk/by-uuid/46d4f3c2-046b-4d59-898c-840b235a30c2";
      fsType = "ext4";
    };

  fileSystems."/boot" =
    { device = "/dev/disk/by-uuid/BCFF-068B";
      fsType = "vfat";
      options = [ "fmask=0077" "dmask=0077" ];
    };

  swapDevices =
    [ { device = "/dev/disk/by-uuid/f8dc0e6c-f024-4361-a829-8a793cb98d75"; }
    ];

  nixpkgs.hostPlatform = lib.mkDefault "x86_64-linux";
  hardware.cpu.amd.updateMicrocode = lib.mkDefault config.hardware.enableRedistributableFirmware;
}
